rules:
  - id: simple-pattern-003-parameter-and-string-wildcard
    languages:
      - python
    severity: ERROR
    message: This example looks for calls to logging.info(get_user() + "") with any user id and any extra string
    pattern: logging.info(get_user(...) + "...")
